from setuptools import find_packages, setup

setup(
    name='bakepy',
    packages=find_packages(include=['bakepy']),
    version='0.0.1',
    description='BakePy Library.',
    author='mg@czacki.com',
    license='NoLicenseYet')